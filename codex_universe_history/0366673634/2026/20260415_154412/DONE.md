# Done

- `orders.csv` đã được ghi thành công cho batch `overnight / pre-open`.
- Batch giữ một phía cuối cùng cho `NVL`: `BUY-only` theo weakness ladder.
- Nguồn quyết định: manifest live bundle, overlay người dùng, research hiện diện, snapshot structured hiện tại và news context mới nhất.
